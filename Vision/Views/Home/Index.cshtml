@model ViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers


@*@if (Model != null && Model.Articles != null) {
    @foreach (var article in Model.Articles) {
		<div style="border: 0.5px solid black; padding: 10px; margin: 5px">
            <h2>@article.name</h2>
            <h3>@article.header</h3>
            <p><i>Автор: </i>@article.author.name</p>
            <p><i>Дата и время: </i>@article.date</p>
            <br /><br />
            <p>@article.text</p>
            <br />
            <a asp-controller="Article" asp-action="Index" asp-route-id="@article.id.ToString()">page link</a>
        </div>
	}
}*@
